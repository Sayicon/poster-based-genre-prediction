# TMDB API kimlik bilgileri ŞABLONU (git'e gider, gerçek değer İÇERMEZ).
# Kullanım: bu dosyayı `.env` olarak kopyala ve değerleri doldur.
#   cp .env.example .env
# Colab'da .env yerine Secrets (🔑) menüsünden TMDB_API_KEY / TMDB_BEARER ekle.

TMDB_API_KEY=
TMDB_BEARER=
